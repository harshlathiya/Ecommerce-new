export function sendOrderConfirmationEmail(_to: string, _orderId: string): void {
  // Integrate SendGrid / SES in production
}

export function sendPasswordResetEmail(_to: string, _token: string): void {
  // Integrate transactional email provider
}
